RewriteEngine On

# Archivos/carpetas reales (assets, img...) se sirven normal
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Cualquier otra ruta -> index.php (SPA)
RewriteRule ^ index.php [L]
